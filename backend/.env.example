# Django Secret Key - REQUIRED (generate with: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())")
DJANGO_SECRET_KEY=your-secure-secret-key-here-change-in-production

# Debug mode (set to False in production)
DEBUG=True

# Supabase Configuration
SUPABASE_URL=https://lmqdnnrbynsjobgsrdgs.supabase.co
SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_JWT_SECRET=your-jwt-secret-here

# Database (required for production, SQLite used in debug if not set)
# DATABASE_URL=postgresql://user:password@host:5432/dbname

# CORS (comma-separated origins for production)
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://www.yourdomain.com

# Allowed Hosts (comma-separated for production)
# ALLOWED_HOSTS=yourdomain.com,www.yourdomain.com
